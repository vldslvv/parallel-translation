{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make build (debug)",
            "type": "shell",
            "command": "make build",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": ["$gcc"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
